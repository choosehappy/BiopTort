{
    "name": "Existing Dockerfile",
    "workspaceFolder": "/opt/BiopTort",
    "build": {
        "context": "..",
        "dockerfile": "../Dockerfile"
    },
    "mounts": [
        "source=${localWorkspaceFolder},target=/opt/BiopTort,type=bind",
        "source=/home/jackson/research/data/dan_problematic_data,target=/data,type=bind"
    ]
}
